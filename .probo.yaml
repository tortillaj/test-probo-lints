steps:
  - name: Update NodeJS and npm
    command: 'cd $SRC_DIR ; sudo npm cache clean -f ; sudo npm install -g n ; sudo n 5.6.0 ; sudo ln -sf /usr/local/n/versions/node/5.6.0/bin/node /usr/bin/node'
  - name: Install deps
    command: 'npm install -g stylelint ; npm install'
  - name: Lint CSS
    command: 'ls $SRC_DIR ; stylelint "./css/*.css"'
