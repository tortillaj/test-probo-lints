steps:
  - name: Linting front-end code
    command: 'sudo npm cache clean -f ; sudo npm install -g n ; sudo n 5.6.0 && sudo ln -sf /usr/local/n/versions/node/5.6.0/bin/node /usr/bin/node ; cd $SRC_DIR ; npm install && npm test'
